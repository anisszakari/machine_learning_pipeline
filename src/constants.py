RANDOM_SEED = 42
VERBOSITY = 3

DEFAULT_NUM_FOLDS = 5

DEFAULT_HYPERPARAMETERS = {
    "objective": "regression",
    "boosting": "gbdt",
    "metric": "rmse",
    "verbosity": 0,
    "learning_rate": 0.1,
    "max_depth": 10,
    "min_child_weight": 10,
}
